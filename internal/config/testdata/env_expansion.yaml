storage:
  dsn: "${TEST_DSN:-postgres://default:default@localhost:5432/logstruct?sslmode=disable}"

sources:
  - name: app
    kind: file
    file:
      path: /tmp/app.log

llm:
  provider: openrouter
  api_key: "${TEST_API_KEY}"
  model: "anthropic/claude-3.5-sonnet"

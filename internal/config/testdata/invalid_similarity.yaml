storage:
  dsn: "postgres://logstruct:logstruct@localhost:5432/logstruct?sslmode=disable"

sources:
  - name: app
    kind: file
    file:
      path: /tmp/app.log

cluster:
  similarity_threshold: 1.5
